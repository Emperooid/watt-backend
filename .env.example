DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Leave unset to use local SQLite. Set to a Postgres URL in production, e.g.
# DATABASE_URL=postgres://user:password@host:5432/dbname
DATABASE_URL=

# Paystack (paid PDF report feature).
PAYSTACK_SECRET_KEY=
FRONTEND_URL=http://localhost:3000

# Email: leave EMAIL_HOST unset to print emails to the console instead of
# actually sending them (fine for local dev).
# EMAIL_HOST=smtp-relay.brevo.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=WattAmIUsing <no-reply@wattamiusing.com>
